{
	"name": "musicvisualizer",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"dev": "npm run dev -w @mv/web",
		"build": "npm run build -w @mv/web",
		"check": "tsc --build packages/core packages/analysis packages/author-engine packages/author-ai packages/transport packages/preview3d && npm run check -w @mv/web",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@types/node": "^26.1.2",
		"typescript": "^6.0.3",
		"vitest": "^4.1.10"
	}
}