{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "What the page is allowed to ask the window for.",
	"windows": ["main"],
	"remote": {
		"comment": "The page is served by the sidecar over http rather than from the bundle, so it counts as a remote origin and is granted nothing by default. The port is chosen at launch, hence the wildcard; the host is not, and loopback is the only origin the window ever opens.",
		"urls": ["http://127.0.0.1:*"]
	},
	"permissions": ["core:window:allow-start-dragging", "core:window:allow-internal-toggle-maximize"]
}
